# M6：数据语义化转换工具链 验收清单

## 数据模型
- [x] TrackFeatures 数据结构包含时间、空间、运动、探测质量、身份属性五类特征字段
- [x] TrackDescription 包含航迹ID、自然语言描述文本、结构化特征数据
- [x] SituationReport 包含所有航迹描述列表和全局态势摘要

## 地理知识库
- [x] 中国主要城市坐标表包含省会、直辖市、重点城市（不少于40个城市）
- [x] 重点区域经纬度范围表包含渤海/黄海/东海/南海海域、台湾海峡等
- [x] 主要机场坐标表包含中国主要机场（不少于20个）
- [x] 坐标→城市名反查功能正常（100km阈值内匹配最近城市）
- [x] 坐标→区域名反查功能正常（经纬度范围判断）
- [x] 坐标→机场名反查功能正常（5km阈值内匹配）
- [x] 无法匹配时降级为坐标描述

## 特征提取
- [x] 能从 FusedTrackPoint 时序数据中正确提取首次/末次出现时间
- [x] 航向转折点检测正确（转向角度 > 30° 的位置被标记为途经点）
- [x] 最大速度、平均速度计算正确（单位转换 m/s → km/h）
- [x] 航向方向转换为八方位描述正确
- [x] 高度范围（最低/最高/平均）计算正确
- [x] 与各传感器的 Haversine 最近距离计算正确
- [x] 航迹质量等级分布统计正确（HIGH/MEDIUM/LOW/LOST 占比）
- [x] 平均信噪比计算正确
- [x] 探测连续性分析正确（断续次数、最长连续时长）
- [x] 敌我属性及置信度提取正确
- [x] SSR 信息（squawk、callsign、ICAO24）提取正确

## 飞行特征推断
- [x] 低空飞行判定正确（平均高度 < 1000m）
- [x] 高空飞行判定正确（平均高度 > 12000m）
- [x] 高速飞行判定正确（最大速度 > 900 km/h）
- [x] 低空突防判定正确（FOE + 低空飞行）
- [x] 多个特征标签可同时生效

## 自然语言生成
- [x] 生成的自然语言描述流畅、完整、信息丰富
- [x] 描述中包含目标批号、起飞信息、途径点、飞行特点、速度、航向、高度、最近传感器距离、降落信息
- [x] 起飞/降落地点通过地理知识库反查为城市名/机场名/区域名
- [x] 无法反查时降级为坐标描述
- [x] 敌我属性描述包含置信度信息
- [x] SSR/ELINT/COMINT 补充信息正确附加到描述末尾

## 全局态势汇总
- [x] 全局摘要包含目标总数、敌方目标数
- [x] 最近目标及距离信息正确
- [x] 最高威胁等级标注正确

## 命令行接口
- [x] `python -m m6_semantic_tool -i <file>` 正常运行
- [x] `-o` 参数输出到文件
- [x] `--format text/json` 切换输出格式
- [x] 无 --m1 参数（不依赖 M1 真值）

## Python API
- [x] `SemanticConverter` 可正确实例化
- [x] `convert_from_file()` 从 M2 综合航迹文件加载并转换
- [x] `convert()` 接受 FusedNetworkTracks 对象直接转换
- [x] `SituationReport.to_text()` 返回完整自然语言文本
- [x] `SituationReport.to_dict()` 返回结构化字典

## 接口兼容性
- [x] 能正确解析 M2 的 fused_comprehensive.json
- [x] 不依赖 M1 真值数据
- [x] 输出格式适合直接输入给 M3 大模型认知中枢使用

## 测试
- [x] 单元测试通过
- [x] 使用 M2 实际输出数据的集成测试通过

## 航迹分段描述（增强）
- [x] 航向转折点检测正确（>15° 触发新飞行段）
- [x] 多段飞行路径描述按时间顺序正确排列
- [x] 每段输出包含时间区间、航向方位角、方向、高度变化

## 高度/速度骤变检测（增强）
- [x] 统计自适应阈值计算正确（mean + 2σ）
- [x] 高度变化绝对值过滤正确（>200m 才标记）
- [x] 速度变化绝对值过滤正确（>50km/h 才标记）
- [x] 异常事件按时间顺序插入到正确飞行段

## 城市途经检测（增强）
- [x] 逐点城市距离计算正确（100km 阈值）
- [x] 途经信息按时间插入到正确飞行段描述

## 雷达站名称映射（增强）
- [x] radar_config.json 中的雷达名称正确加载
- [x] 输出中使用"青岛雷达"等中文名称而非"RADAR_01"

## 机场级起降识别（增强）
- [x] 机场坐标表包含不少于30个机场
- [x] 机场优先匹配（5km 阈值）正确
- [x] 机场→城市→区域→坐标 降级链正确

## 中文时间格式（增强）
- [x] 时间输出格式为"14时21分48秒"而非"14:21:48"

## M4 界面集成（新增）
- [x] Flask 服务器 (`m4_situation_visualization/server.py`) 可正常启动
- [x] `/api/m6/convert` 接口返回正确的 JSON 结果
- [x] `/api/m6/save` 接口可手动保存结果
- [x] 转化结果自动保存到 `output/m6_semantic_tool/` 目录
- [x] 接口支持 track_id 过滤单条航迹
- [x] 项目总体说明文档 M6 部分已更新
